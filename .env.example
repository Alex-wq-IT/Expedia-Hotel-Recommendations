# Copy to .env, replace the placeholder, then export these variables before
# running both Docker Compose and the host-side BI publisher.
CLICKHOUSE_USER=expedia_bi
CLICKHOUSE_PASSWORD=replace-with-a-non-empty-local-or-deployment-secret
